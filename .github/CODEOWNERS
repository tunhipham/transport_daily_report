# ═══════════════════════════════════════════════════════════
# CODEOWNERS — Bảo vệ dashboard architecture
# ═══════════════════════════════════════════════════════════
# Mọi thay đổi vào file dưới đây BẮT BUỘC phải có review
# từ @tunhipham trước khi merge vào main.
#
# External collaborator KHÔNG ĐƯỢC sửa trực tiếp các file này.
# ═══════════════════════════════════════════════════════════

# ── Dashboard master layout — CẤM external sửa ──
docs/index.html                     @tunhipham

# ── Data folder — chỉ pipeline nội bộ được sửa ──
docs/data/                          @tunhipham

# ── Scripts — chỉ owner ──
script/                             @tunhipham

# ── Config — chỉ owner ──
config/                             @tunhipham

# ── Agents/workflows — chỉ owner ──
.agents/                            @tunhipham
agents/                             @tunhipham

# ── External content — managed by auto-approve workflow ──
# Whitelisted files (nhap_xuat_dm.html, claim_aba.html) auto-approve qua CI.
# File MỚI trong docs/external/ sẽ bị workflow BLOCK → cần owner duyệt thủ công.
# KHÔNG liệt kê docs/external/ ở đây để github-actions[bot] approve là đủ.
