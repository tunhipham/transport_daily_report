# Session Summary — 27/05/2026

**Dự án**: Transport Daily Report (Performance Dashboard)

Trong session làm việc ngày hôm nay, chúng ta đã biến Dashboard Performance từ dạng "Báo cáo cuối tháng" thành một **Hệ thống theo dõi và cảnh báo Realtime**. 

Dưới đây là tóm tắt toàn bộ công việc đã hoàn thiện:

## 1. Thiết lập Automation & Realtime Data Pipeline
- Thay thế luồng data Excel thủ công bằng **ClickHouse Realtime** (`fetch_db_realtime.py`).
- Cấu hình **Task Scheduler** chạy tự động luồng dữ liệu mỗi 30 phút.
- Tự động hóa incremental fetch từ Google Sheet kế hoạch vận tải.
- Data xuất ra file `performance.json` và Deploy tự động lên GitHub Pages qua `deploy.py` (Kèm bảo mật chặn external tamper).

## 2. Hệ thống cảnh báo qua Telegram (Dzú trip)
- **Monday Reminder**: Tạo script `trip_reminder.py` chạy vào 08:00 T2+T3. Thống kê siêu thị nào chưa được giao hàng, chia theo Kho và gửi thẳng qua Telegram cá nhân để anh trực tiếp "dzí" tài xế/hub.
- **Tuesday Cutoff**: Tạo script `trip_cutoff_export.py` chạy vào 09:00 T3. Tự động xuất file Excel Final (Performance + SLA tuần) và bắn file thẳng vào Telegram để kết sổ.

## 3. Tính năng Tracking Realtime & Lịch sử trên Dashboard
- Thêm section **"Tracking Realtime"** mới toanh ngay trên tab Performance, giúp theo dõi live (Ngày D) xem xe nào đã giao, xe nào chưa giao.
- **Hỗ trợ truy xuất lịch sử**: Khung chọn ngày được thiết kế dạng Dropdown menu, tự động gom toàn bộ tracking data từ đầu tháng (ví dụ từ 01/05 -> 27/05). Người dùng có thể tua ngược lại bất kỳ ngày nào để xem lịch sử hành trình & giao nhận của ngày hôm đó (với tổng số dòng tracking lên tới ~15.000 rows cho 1 tháng).
- **Giao diện**: Chia 6 tab kho (KRC, ĐÔNG, MÁT, KSL-Sáng, KSL-Tối, THỊT CÁ) với UI màu sắc highlight trạng thái trực quan.
- **Tích hợp Giờ dự kiến**: Viết script `push_compose_plan.py` để "móc" giờ dự kiến giao từ luồng Compose-mail (chạy từ hôm trước) và đắp vào Dashboard realtime.
- **Phân rã Rổ/Tote vs Thùng/Kiện**: Query `tli_transfer_qty` và `tli_received_qty` từ ClickHouse, kết hợp regex bóc tách tên container. Người dùng chỉ cần hover chuột vào tổng số lượng trên web, tool-tip sẽ pop-up chi tiết Rổ/Tote và Thùng/Kiện.
- **Ngoại lệ Thịt Cá**: Tự động ẩn 2 cột số lượng container cho riêng kho Thịt Cá đúng như request.

## 4. Tài liệu hoá (Documentation)
- Cập nhật file `.agents\workflows\performance-report.md` phân chia rõ ràng Mode 1 (Realtime) và Mode 2 (Manual).
- Document lại đầy đủ cách kích hoạt/tắt Task Scheduler, các lệnh CLI chạy manual, và luồng hoạt động của tính năng Tracking mới.

---
**Next steps (nếu có)**: Tính năng hoạt động auto 100%. Nếu sau này có kho mới hoặc đổi tên loại rổ/thùng, chỉ cần update biến `CARTON_KEYWORDS` trong `fetch_db_realtime.py`.
